Your goal is to implement the project desceribed in @Final Project.pdf . You may reference any docs in the @lab_files/ diretory for support in this matter.

Your goal is performanec, and while the lab does not determine perfromance metrics, a faster simulation time with lower peak and average temperataures is a good way to evaluate the performance of a solution. Your goal is to iterate repetiively until you have accomplished a signifigant improvement over the curretn approach. work on the simulator_simulate method to accomplish this. Feel free to break the solution for simulator_simulate out into a new file to be more visibile. In this case the solution would be implemented in a new file and called from the therm.py file. 

Make sure to reference @docs/ for gotchas and project advice as well as critical anemoi-reference.md for refererence from the old code solution that the TA gave as an example, containing important thermal information.

Ensure all tests are supported via scripts. Ensure scripts and python files exist that are able to corretly evaluate and output results. Ensure result visualization is correct and consistent with the designs, ensure that the outptus are cor5rercctly generated. Utilities that use the output results are great and if they can help us debug a solution, visualize its implementation, and iterate on designs all the better.

Keep iterating and testing until the job is done. When running tests, create a script and use that script to run tests and do not just run console ommands. Scripts exist in @scripts/ .

Wirte documentation explaining the design process please. In the project it syas we need to wriute a report, at teh end a docc emulating a report needs to be generated that would compregnsizvely cover the requirerments of the report. Otyher fiels documenting process, scripts, changes, and choices are also preferred.